# Cattle Re-ID dependencies
#
# Environment: use the `cattle` conda env (NOT base):
#   conda activate cattle
#
# GPU training/inference (recommended, RTX 5080):
#   torch 2.11 + cu128 + timm are already installed in the cattle env.
#   For a fresh Colab-style env:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# The OSNet zero-shot pipeline (cattle_osnet/) also works on CPU.

# Core
torch>=2.1.0
torchvision>=0.16.0
numpy

# ViT + PHE training pipeline (training/train_v3.py, test_model_v3.py,
# video_reid_v5.py, export.py)
timm
scipy

# Computer vision
ultralytics>=8.3.0
opencv-python
Pillow
albumentations

# Utilities
matplotlib
tqdm
huggingface_hub

# ONNX export (training/export.py)
onnx
onnxruntime

# Tests
pytest

# Optional: hard-negative mining in experiments/contrastive_pretrain.py
pytorch_metric_learning

# NOTE: the legacy torchreid OSNet pipeline (training/train.py, evaluate.py)
# needs deep-person-reid from GitHub (NOT the fake PyPI `torchreid` package):
#   pip install git+https://github.com/KaiyangZhou/deep-person-reid.git